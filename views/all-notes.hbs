<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Todas las Notas</title>
    {{> head}}
</head>
<body class="profile">
    {{> navigation}}
    {{> errors}}
    {{> messages}}
    <div class="row p-4">
        {{#each notas}}
        <div class="col-md-3 p-2">
            <div class="card">
                <div class="card-body">
                    <h4 class="card-title d-flex justify-content-between align-items-center">
                    {{ titulo }} <a href="/nota-editar/{{_id}}"><i class="fas fa-edit"></i></a>
                    </h4>
                    <p>{{ descripcion }} </p>
                    <!-- DELETE REQUEST -->
                    <form action="/nota-eliminar/{{_id}}?_method=DELETE" method="POST">
                    <input type="hidden" name="_method" value="DELETE">
                    <button type="submit" class="btn btn-danger btn-block btn-sm">Eliminar</button>
                    </form>
                </div>
            </div>
        </div>
        {{else}}
        <div class="card mx-auto">
            <div class="card-body">
            <h1>Hola {{user.nombre}}</h1>
            <p clsss="lead">No hay ninguna Nota todavia.</p>
            <a href="/nota-nueva" class="btn btn-success btn-block">Crea una Nota!</a>
            </div>
        </div>
        {{/each}}
    </div>
    {{> scripts}}
</body>
</html>