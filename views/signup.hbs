<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Sign Up</title>
    {{> head}}
</head>
<body class="signup">
    <main class="container p-4">
    {{> errors}}
    {{> messages}}
    <div class="container">
        <div class="col-sm-6 mx-auto">
            <div class="card mt-5">
                <div class="card-body">
                    <h1 class="text-center">
                        <span class="fa fa-sign-in"></span>  Registrarse
                    </h1>
                    <form action="/signup" method="POST">
                        <div class="form-group">
                            {{!-- <label for="email">Nombre y Apellido</label> --}}
                            <input type="text" class="form-control" name="nombre" placeholder="Nombre y Apellido" value="{{body.nombre}}">
                        </div>
                        <div class="form-group">
                            {{!-- <label for="email">Email</label> --}}
                            <input type="email" class="form-control" name="email" placeholder="Email" value="{{body.email}}">
                        </div>
                        <div class="form-group">
                            {{!-- <label for="password">Password</label> --}}
                            <input type="password" class="form-control" name="password" placeholder="Contraseña" value="{{body.password}}">
                        </div>
                        <button class="btn btn-dark btn-block">
                            Enviar Datos 
                        </button>
                    </form>
                    <hr>
                    <div class="text-center">
                        <p>Si ya tienes una cuenta? <a href="/signin">Ingresar</a></p>
                        <p>O ir a <a href="/">Home</a>.</p>
                    </div>
                </div>
            </div>
        </div>
    </div>
    </main>
    {{> scripts}}
</body>
</html>